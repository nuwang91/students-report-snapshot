import { NuguResultColorDirective } from './result-color.directive';

describe('NuguResultColorDirective', () => {
  it('should create an instance', () => {
    const directive = new NuguResultColorDirective();
    expect(directive).toBeTruthy();
  });
});
