import { NuguWidthDirective } from './width.directive';

describe('NuguWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new NuguWidthDirective();
    expect(directive).toBeTruthy();
  });
});
